<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.baic.navi"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="28"
        android:targetSdkVersion="34" />

    <!-- Android 11+ 包可见性：声明要 bind 的目标包，否则查询/绑定不到（§4） -->
    <queries>
        <package android:name="com.baic.usercenter" />
        <package android:name="com.baic.media" />
    </queries>

    <permission
        android:name="com.baic.bridge.permission.LINK"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.baic.bridge.permission.LINK" />

    <application
        android:name="com.baic.navi.NaviApp"
        android:allowBackup="false"
        android:debuggable="true"
        android:extractNativeLibs="false"
        android:label="导航(账号Consumer)" >
        <activity
            android:name="com.baic.navi.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>