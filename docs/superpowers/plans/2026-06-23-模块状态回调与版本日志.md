# 模块状态回调 + 非就绪告警 + 版本日志 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 Bridge SDK 增加模块状态回调（onConnected/onReady/onRebooted）+ isReady 查询、非就绪调用 request 的告警日志、以及启动时打印 SDK/契约门面/git 版本信息。

**Architecture:** 改动集中在 `:core:lite`。把「topic→module」与「就绪状态机」抽成纯 Java 类（`Topics`、`ModuleState`）做 JVM 单测；`BridgeCore` 持有每模块 `ModuleState`，在握手/连接/断开三处统一 post 到 worker 线程重算并触发回调。版本信息由 `:core:lite` 的 `BuildConfig`（gradle 构建时注入 git hash）提供，契约门面版本由 `register(module, version, cb)` 传入打印。不触碰 `:transport` AIDL 方法签名（仅加常量）。

**Tech Stack:** Java 17、Android Library（AGP 8.1.4 / Gradle 8.9）、JUnit 4（纯 JVM 单测）。

**构建/测试环境：** `export JAVA_HOME=~/Library/Java/JavaVirtualMachines/liberica-17.0.18`（命令行构建必须用标准 JDK 17，AS 自带 JBR 会让 jlink 失败）。

**设计依据：** `docs/superpowers/specs/2026-06-23-模块状态回调与版本日志-design.md`

---

### Task 1: `Topics.moduleOf` 工具（TDD）+ 单测基建

**Files:**
- Modify: `core/lite/build.gradle.kts`
- Create: `core/lite/src/test/java/com/baic/bridge/core/TopicsTest.java`
- Create: `core/lite/src/main/java/com/baic/bridge/core/Topics.java`

- [ ] **Step 1: 给 `:core:lite` 加 JUnit 测试依赖**

修改 `core/lite/build.gradle.kts`，在 `dependencies` 块加测试依赖（保留原 `api(project(":transport"))`）：

```kotlin
dependencies {
    api(project(":transport"))
    testImplementation("junit:junit:4.13.2")
}
```

- [ ] **Step 2: 写失败测试**

创建 `core/lite/src/test/java/com/baic/bridge/core/TopicsTest.java`：

```java
package com.baic.bridge.core;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertNull;

import org.junit.Test;

public class TopicsTest {
    @Test public void 取首个点前的前缀() {
        assertEquals("media", Topics.moduleOf("media.play"));
        assertEquals("usercenter", Topics.moduleOf("usercenter.account.state"));
    }
    @Test public void 无点时返回原串() {
        assertEquals("media", Topics.moduleOf("media"));
    }
    @Test public void null返回null() {
        assertNull(Topics.moduleOf(null));
    }
}
```

- [ ] **Step 3: 运行测试确认失败**

Run: `./gradlew :core:lite:testDebugUnitTest`
Expected: 编译失败 / FAIL —— `Topics` 不存在。

- [ ] **Step 4: 写最小实现**

创建 `core/lite/src/main/java/com/baic/bridge/core/Topics.java`：

```java
package com.baic.bridge.core;

/** topic ↔ module 工具。module = topic 首个 '.' 前的前缀（与 contract 的前缀约定一致）。 */
final class Topics {
    /** 取 topic 的模块前缀；null→null；无 '.' 原样返回。 */
    static String moduleOf(String topic) {
        if (topic == null) return null;
        int i = topic.indexOf('.');
        return i < 0 ? topic : topic.substring(0, i);
    }

    private Topics() {}
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `./gradlew :core:lite:testDebugUnitTest`
Expected: PASS（3 个用例）。

- [ ] **Step 6: 提交**

```bash
git add core/lite/build.gradle.kts core/lite/src/test/java/com/baic/bridge/core/TopicsTest.java core/lite/src/main/java/com/baic/bridge/core/Topics.java
git commit -m "feat(core): 新增 Topics.moduleOf 工具 + JUnit 单测基建"
```

---

### Task 2: `ModuleCallback` 接口 + `ModuleState` 就绪状态机（TDD）

**Files:**
- Create: `core/lite/src/main/java/com/baic/bridge/core/ModuleCallback.java`
- Create: `core/lite/src/main/java/com/baic/bridge/core/ModuleState.java`
- Create: `core/lite/src/test/java/com/baic/bridge/core/ModuleStateTest.java`

- [ ] **Step 1: 新增 `ModuleCallback` 接口（无 Android 依赖，可被纯 JVM 测试引用）**

创建 `core/lite/src/main/java/com/baic/bridge/core/ModuleCallback.java`：

```java
package com.baic.bridge.core;

/** 模块状态回调。所有方法在 SDK worker 线程串行触发，更新 UI 请自行切主线程。 */
public interface ModuleCallback {
    /** 提供该模块的节点 bindService 成功（传输通道建立，尚未握手）。排查日志用。 */
    default void onConnected() {}

    /** 提供方握手完成、该模块能力首次可用。 */
    void onReady();

    /** 提供方重启恢复（曾 ready → 断开 → 重连重握手）后再次可用。 */
    default void onRebooted() {}
}
```

- [ ] **Step 2: 写失败测试**

创建 `core/lite/src/test/java/com/baic/bridge/core/ModuleStateTest.java`：

```java
package com.baic.bridge.core;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertTrue;

import org.junit.Test;

public class ModuleStateTest {
    @Test public void 首次就绪触发READY() {
        ModuleState st = new ModuleState("media", 1);
        assertEquals(ModuleState.Event.READY, st.applyReadiness(true));
        assertTrue(st.isReady());
    }

    @Test public void 已就绪再次就绪无事件() {
        ModuleState st = new ModuleState("media", 1);
        st.applyReadiness(true);
        assertEquals(ModuleState.Event.NONE, st.applyReadiness(true));
    }

    @Test public void 就绪后掉线触发LOST且isReady转false() {
        ModuleState st = new ModuleState("media", 1);
        st.applyReadiness(true);
        assertEquals(ModuleState.Event.LOST, st.applyReadiness(false));
        assertFalse(st.isReady());
    }

    @Test public void 掉线后再就绪触发REBOOTED() {
        ModuleState st = new ModuleState("media", 1);
        st.applyReadiness(true);   // READY
        st.applyReadiness(false);  // LOST
        assertEquals(ModuleState.Event.REBOOTED, st.applyReadiness(true));
        assertTrue(st.isReady());
    }

    @Test public void 从未就绪掉线判定无事件() {
        ModuleState st = new ModuleState("media", 1);
        assertEquals(ModuleState.Event.NONE, st.applyReadiness(false));
    }
}
```

- [ ] **Step 3: 运行测试确认失败**

Run: `./gradlew :core:lite:testDebugUnitTest`
Expected: 编译失败 —— `ModuleState` 不存在。

- [ ] **Step 4: 写最小实现**

创建 `core/lite/src/main/java/com/baic/bridge/core/ModuleState.java`：

```java
package com.baic.bridge.core;

import java.util.concurrent.CopyOnWriteArrayList;

/**
 * 单个已注册模块的就绪状态 + 回调集合。
 * 状态转移为纯逻辑（不碰 Android / 不触发回调），便于 JVM 单测；回调由 BridgeCore 据返回事件触发。
 */
final class ModuleState {
    /** applyReadiness 返回的应触发事件。 */
    enum Event { NONE, READY, REBOOTED, LOST }

    final String module;
    final int contractVersion;                 // 契约门面版本，仅用于日志
    final CopyOnWriteArrayList<ModuleCallback> callbacks = new CopyOnWriteArrayList<>();

    private boolean readyOnce;                  // 历史上是否就绪过
    private boolean currentlyReady;             // 当前是否有握手完成的提供方

    ModuleState(String module, int contractVersion) {
        this.module = module;
        this.contractVersion = contractVersion;
    }

    boolean isReady() { return currentlyReady; }

    /** 依据最新就绪判定推进状态，返回应触发的事件。线程安全（worker 串行调用，仍加锁兜底）。 */
    synchronized Event applyReadiness(boolean now) {
        if (!currentlyReady && now) {
            currentlyReady = true;
            if (!readyOnce) { readyOnce = true; return Event.READY; }
            return Event.REBOOTED;
        }
        if (currentlyReady && !now) {
            currentlyReady = false;
            return Event.LOST;
        }
        return Event.NONE;
    }
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `./gradlew :core:lite:testDebugUnitTest`
Expected: PASS（含 Task 1 的 3 个 + 本任务 5 个）。

- [ ] **Step 6: 提交**

```bash
git add core/lite/src/main/java/com/baic/bridge/core/ModuleCallback.java core/lite/src/main/java/com/baic/bridge/core/ModuleState.java core/lite/src/test/java/com/baic/bridge/core/ModuleStateTest.java
git commit -m "feat(core): 新增 ModuleCallback 接口 + ModuleState 就绪状态机（含单测）"
```

---

### Task 3: 节点清单解析 modules 字段

**Files:**
- Modify: `core/lite/src/main/java/com/baic/bridge/core/NodeDescriptor.java`
- Modify: `core/lite/src/main/java/com/baic/bridge/core/NodeRegistry.java`

> 说明：`NodeDescriptor` 的构造器目前仅被 `NodeRegistry` 调用，改 4 参安全。

- [ ] **Step 1: `NodeDescriptor` 增 `modules` 字段**

将 `core/lite/src/main/java/com/baic/bridge/core/NodeDescriptor.java` 整体替换为：

```java
package com.baic.bridge.core;

import java.util.Collections;
import java.util.Set;

/** 静态清单中的一个节点条目（Bridge SDK §4.1）。 */
public final class NodeDescriptor {
    public final String id;         // 节点 id = 包名
    public final String action;     // bind 用的 intent action
    public final String component;  // 形如 "pkg/.HostService"；为空则按 action 隐式解析（全量自带 Service）
    public final Set<String> modules; // 该节点提供的模块（用于 onConnected 归属）；缺省空集

    public NodeDescriptor(String id, String action, String component, Set<String> modules) {
        this.id = id; this.action = action; this.component = component;
        this.modules = modules != null ? modules : Collections.emptySet();
    }
}
```

- [ ] **Step 2: `NodeRegistry` 解析 `modules` 数组**

在 `core/lite/src/main/java/com/baic/bridge/core/NodeRegistry.java` 中，将 for 循环体（当前 30-36 行的 `out.add(new NodeDescriptor(...))`）替换为：

```java
                for (int i = 0; i < nodes.length(); i++) {
                    JSONObject o = nodes.getJSONObject(i);
                    java.util.Set<String> mods = new java.util.LinkedHashSet<>();
                    JSONArray ma = o.optJSONArray("modules");
                    if (ma != null) for (int j = 0; j < ma.length(); j++) {
                        String m = ma.optString(j, null);
                        if (m != null && !m.isEmpty()) mods.add(m);
                    }
                    out.add(new NodeDescriptor(
                            o.optString("id", null),
                            o.optString("action", "com.baic.bridge.NODE"),
                            o.has("component") ? o.optString("component", null) : null,
                            mods));
                }
```

- [ ] **Step 3: 编译验证**

Run: `./gradlew :core:lite:compileDebugJavaWithJavac`
Expected: BUILD SUCCESSFUL。

- [ ] **Step 4: 提交**

```bash
git add core/lite/src/main/java/com/baic/bridge/core/NodeDescriptor.java core/lite/src/main/java/com/baic/bridge/core/NodeRegistry.java
git commit -m "feat(core): 节点清单支持 modules 字段（module→节点映射）"
```

---

### Task 4: `:transport` 增 ABI 常量（不改方法签名）

**Files:**
- Modify: `transport/src/main/java/com/baic/bridge/transport/BridgeEnvelope.java:19`

- [ ] **Step 1: 加常量**

在 `BridgeEnvelope.java` 第 19 行 `public static final int TYPE_ERROR = 5;` 之后插入一行：

```java
    public static final int TYPE_ERROR    = 5;

    // ── 传输 ABI 版本（仅常量，不改方法签名，不破坏冻结）──
    public static final int ABI_VERSION   = 1;
```

- [ ] **Step 2: 编译验证**

Run: `./gradlew :transport:compileDebugJavaWithJavac`
Expected: BUILD SUCCESSFUL。

- [ ] **Step 3: 提交**

```bash
git add transport/src/main/java/com/baic/bridge/transport/BridgeEnvelope.java
git commit -m "feat(transport): 新增 ABI_VERSION 常量（不动 AIDL 签名）"
```

---

### Task 5: `:core:lite` 注入 BuildConfig 版本信息

**Files:**
- Modify: `core/lite/build.gradle.kts`

- [ ] **Step 1: 开启 buildConfig 并注入 SDK_VERSION/GIT_SHA/BUILD_TIME**

将 `core/lite/build.gradle.kts` 整体替换为：

```kotlin
// ═══ Bridge 内核·lite ═══ 无 Android Service 组件，提供发现/连接/重连/编解码/RPC/分发/鉴权，
// 统一静态门面 Bridge（宿主可在已有 Service.onBind 返回 Bridge.nodeBinder() 外挂）。全量 core 在此基础上加托管 Service。
import java.io.ByteArrayOutputStream

plugins { id("com.android.library") }

// 构建时取 git 短 hash；无 git / 失败兜底 "unknown"，不让构建中断。
fun gitShortSha(): String = try {
    val out = ByteArrayOutputStream()
    exec {
        commandLine("git", "rev-parse", "--short", "HEAD")
        standardOutput = out
        isIgnoreExitValue = true
    }
    out.toString().trim().ifEmpty { "unknown" }
} catch (e: Exception) { "unknown" }

val sdkVersion = "1.0.0"
val gitSha = gitShortSha()
val buildTime: String = java.time.format.DateTimeFormatter.ISO_INSTANT.format(java.time.Instant.now())

android {
    namespace = "com.baic.bridge.core"; compileSdk = 34
    defaultConfig {
        minSdk = 28
        buildConfigField("String", "SDK_VERSION", "\"$sdkVersion\"")
        buildConfigField("String", "GIT_SHA", "\"$gitSha\"")
        buildConfigField("String", "BUILD_TIME", "\"$buildTime\"")
    }
    buildFeatures { buildConfig = true }
    compileOptions { sourceCompatibility = JavaVersion.VERSION_17; targetCompatibility = JavaVersion.VERSION_17 }
}
dependencies {
    api(project(":transport"))
    testImplementation("junit:junit:4.13.2")
}
```

- [ ] **Step 2: 编译验证 BuildConfig 生成**

Run: `./gradlew :core:lite:compileDebugJavaWithJavac`
Expected: BUILD SUCCESSFUL，生成 `com.baic.bridge.core.BuildConfig`（含 SDK_VERSION/GIT_SHA/BUILD_TIME）。

- [ ] **Step 3: 提交**

```bash
git add core/lite/build.gradle.kts
git commit -m "build(core): 注入 SDK_VERSION/GIT_SHA/BUILD_TIME 到 BuildConfig"
```

---

### Task 6: `BridgeCore` 接入模块状态机 + 启动横幅 + request 告警

**Files:**
- Modify: `core/lite/src/main/java/com/baic/bridge/core/BridgeCore.java`

> 本任务在一个文件内多处改动，逐步替换。每步给出完整目标代码。

- [ ] **Step 1: 新增字段（在第 50 行 `subs` 声明之后插入）**

在 `private final CopyOnWriteArrayList<Sub> subs = new CopyOnWriteArrayList<>();` 之后插入：

```java
    // 每个已注册模块的就绪状态 + 回调
    private final ConcurrentHashMap<String, ModuleState> moduleStates = new ConcurrentHashMap<>();
    // 节点id → 它提供的模块（来自静态清单），仅用于 onConnected 归属
    private final ConcurrentHashMap<String, java.util.Set<String>> nodeModules = new ConcurrentHashMap<>();
```

- [ ] **Step 2: 改 `start()`（当前 83-87 行）—— 建映射 + 启动横幅**

将 `start()` 方法整体替换为：

```java
    void start() {
        List<NodeDescriptor> nodes = NodeRegistry.load(ctx);
        for (NodeDescriptor n : nodes) {
            if (n.id != null && !n.modules.isEmpty()) nodeModules.put(n.id, n.modules);
        }
        connections.connectAll(nodes, selfId);
        Log.i(TAG, "==== Bridge SDK 启动 ==== self=" + selfId);
        Log.i(TAG, "SDK版本=" + BuildConfig.SDK_VERSION + " gitSha=" + BuildConfig.GIT_SHA
                + " 构建=" + BuildConfig.BUILD_TIME + " 传输ABI=" + BridgeEnvelope.ABI_VERSION
                + " 清单节点数=" + nodes.size());
    }
```

- [ ] **Step 3: 改 `binderDied`（当前 102-108 行）—— 死亡后重算模块**

将 `linkDeath` 里的 `binderDied()` 方法体替换为：

```java
                @Override public void binderDied() {
                    Log.w(TAG, "提供方死亡 " + pc.peerId + "，清理路由 + 失败该对端在途请求");
                    worker.execute(() -> {
                        connections.peers.remove(pc.peerId);
                        rpc.failPeer(pc.peerId, BridgeErrors.E_NOT_CONNECTED, "对端已断开");
                        reevaluateAll();
                    });
                }
```

- [ ] **Step 4: 改 `handleHello`（当前 131-142 行）—— 握手后重算模块**

将 `handleHello` 的 try 块内、`Log.i(TAG, "握手 from ...")` 之后追加 `reevaluateAll();`，即整体替换为：

```java
    private void handleHello(BridgeEnvelope env) {
        PeerConnection pc = connections.peers.get(env.source);
        if (pc == null) return;   // attach 尚未建立，等对端 attach 后会补握手
        try {
            JSONObject o = new JSONObject(env.payload == null ? "{}" : env.payload);
            applyTopics(pc.providedTopics, o.optJSONArray("provide"));
            applyTopics(pc.subscribedTopics, o.optJSONArray("subscribe"));
            Log.i(TAG, "握手 from " + env.source + " provide=" + pc.providedTopics + " subscribe=" + pc.subscribedTopics);
            reevaluateAll();   // 提供方能力到位，重算各模块就绪
        } catch (Exception e) {
            Log.w(TAG, "解析 HELLO 失败 " + e);
        }
    }
```

- [ ] **Step 5: 改 `register`（当前 199 行）+ 新增 register 重载 / isReady / 模块评估方法**

将当前 `void register(String module) { if (module != null) modules.add(module); }`（199 行）整体替换为下列一组方法：

```java
    void register(String module) { register(module, 0, null); }

    void register(String module, int contractVersion, ModuleCallback cb) {
        if (module == null) return;
        modules.add(module);
        ModuleState st = moduleStates.computeIfAbsent(module, m -> new ModuleState(m, contractVersion));
        if (cb != null) st.callbacks.add(cb);
        Log.i(TAG, "注册模块 " + module + " 契约门面版本=" + contractVersion + " callback=" + (cb != null));
        worker.execute(() -> reevaluate(module));   // 提供方可能已就绪，补一次评估
    }

    boolean isReady(String module) {
        ModuleState st = moduleStates.get(module);
        return st != null && st.isReady();
    }

    /** 该模块是否存在握手完成、provide 了其前缀 topic 的提供方。 */
    private boolean evaluateReady(String module) {
        String prefix = module + ".";
        for (PeerConnection pc : connections.peers.values()) {
            for (String t : pc.providedTopics) if (t != null && t.startsWith(prefix)) return true;
        }
        return false;
    }

    /** worker 线程：推进单模块状态并触发回调/日志。 */
    private void reevaluate(String module) {
        ModuleState st = moduleStates.get(module);
        if (st == null) return;
        switch (st.applyReadiness(evaluateReady(module))) {
            case READY:
                Log.i(TAG, "模块就绪 " + module);
                for (ModuleCallback cb : st.callbacks) safe(cb::onReady);
                break;
            case REBOOTED:
                Log.i(TAG, "模块提供方重启恢复 " + module);
                for (ModuleCallback cb : st.callbacks) safe(cb::onRebooted);
                break;
            case LOST:
                Log.w(TAG, "模块提供方已离线 " + module);
                break;
            default: break;
        }
    }

    private void reevaluateAll() {
        for (String m : moduleStates.keySet()) reevaluate(m);
    }

    /** bind 成功（onServiceConnected）—— 对该节点提供的模块触发 onConnected（排查日志用）。 */
    void onPeerConnected(String nodeId) {
        worker.execute(() -> {
            java.util.Set<String> mods = nodeModules.get(nodeId);
            if (mods == null) return;
            for (String m : mods) {
                ModuleState st = moduleStates.get(m);
                if (st != null) {
                    Log.i(TAG, "模块节点已连接 module=" + m + " node=" + nodeId);
                    for (ModuleCallback cb : st.callbacks) safe(cb::onConnected);
                }
            }
        });
    }

    /** 连接断开（onServiceDisconnected）—— 移除并重算模块。 */
    void onPeerLost(String nodeId) {
        worker.execute(() -> {
            connections.peers.remove(nodeId);
            reevaluateAll();
        });
    }

    private static void safe(Runnable r) {
        try { r.run(); } catch (Exception e) { Log.w(TAG, "模块回调异常 " + e); }
    }
```

- [ ] **Step 6: 改 `request`（当前 227-234 行）—— 非就绪告警**

将 `request` 方法整体替换为：

```java
    void request(String topic, String payload, BridgeReply reply, long timeoutMs) {
        PeerConnection provider = findProvider(topic);
        if (provider == null) {
            String module = Topics.moduleOf(topic);
            Log.w(TAG, "request 调用时模块未就绪 topic=" + topic + " module=" + module
                    + " isReady=" + isReady(module)
                    + "，当前无提供方，返回 E_NO_PROVIDER；建议等 onReady 回调后再调用");
            reply.onError(BridgeErrors.E_NO_PROVIDER, "无节点提供 " + topic);
            return;
        }
        String corr = UUID.randomUUID().toString();
        rpc.register(corr, provider.peerId, reply, timeoutMs);
        boolean sent = provider.send(newEnv(BridgeEnvelope.TYPE_REQUEST, topic, payload, corr, 0));
        if (!sent) rpc.complete(corr, BridgeErrors.E_NOT_CONNECTED, "投递失败，对端通道已断");
    }
```

- [ ] **Step 7: 编译验证**

Run: `./gradlew :core:lite:compileDebugJavaWithJavac`
Expected: BUILD SUCCESSFUL。

- [ ] **Step 8: 提交**

```bash
git add core/lite/src/main/java/com/baic/bridge/core/BridgeCore.java
git commit -m "feat(core): 模块就绪状态机接入握手/连接/断开 + 启动横幅 + request 非就绪告警"
```

---

### Task 7: `ConnectionManager` 把连接/断开事件路由给 BridgeCore

**Files:**
- Modify: `core/lite/src/main/java/com/baic/bridge/core/ConnectionManager.java:56-71`

- [ ] **Step 1: onServiceConnected 末尾通知 + onServiceDisconnected 改路由**

将 `ServiceConnection conn = new ServiceConnection() { ... }`（当前 56-71 行）整体替换为：

```java
        final ServiceConnection conn = new ServiceConnection() {
            @Override public void onServiceConnected(ComponentName name, IBinder binder) {
                backoff.remove(n.id);
                IBridgeNode remote = IBridgeNode.Stub.asInterface(binder);
                PeerConnection pc = new PeerConnection(n.id, remote);
                peers.put(n.id, pc);
                core.linkDeath(pc);
                core.attachTo(remote);     // 双向：把本端回调通道交给对端
                core.sendHelloTo(pc);      // 声明本端 provide/subscribe
                core.onPeerConnected(n.id); // bind 成功 → 触发该节点模块的 onConnected（排查日志）
                Log.i(TAG, "已连接 " + n.id);
            }
            @Override public void onServiceDisconnected(ComponentName name) {
                Log.w(TAG, "连接断开 " + n.id + "（等待 BIND_AUTO_CREATE 自动恢复）");
                core.onPeerLost(n.id);     // 移除 + 重算模块就绪
            }
        };
```

- [ ] **Step 2: 编译验证**

Run: `./gradlew :core:lite:compileDebugJavaWithJavac`
Expected: BUILD SUCCESSFUL。

- [ ] **Step 3: 提交**

```bash
git add core/lite/src/main/java/com/baic/bridge/core/ConnectionManager.java
git commit -m "feat(core): 连接/断开事件路由到 BridgeCore 模块状态机"
```

---

### Task 8: `Bridge` 门面暴露新 API

**Files:**
- Modify: `core/lite/src/main/java/com/baic/bridge/core/Bridge.java:26`

- [ ] **Step 1: 新增 register 重载 + isReady（保留旧 `register(String)`）**

将当前 `public static void register(String module) { core().register(module); }`（26 行）整体替换为：

```java
    /** 注册模块（声明关心该模块、打开收发开关）。 */
    public static void register(String module) { core().register(module); }

    /** 注册模块并监听状态（onConnected/onReady/onRebooted）。契约门面版本未知时记 0。 */
    public static void register(String module, ModuleCallback callback) {
        core().register(module, 0, callback);
    }

    /** 注册模块并监听状态，同时上报契约门面版本（供启动/排查日志）。 */
    public static void register(String module, int contractVersion, ModuleCallback callback) {
        core().register(module, contractVersion, callback);
    }

    /** 查询模块是否就绪（提供方已连接且握手完成）。 */
    public static boolean isReady(String module) { return core().isReady(module); }
```

- [ ] **Step 2: 编译验证**

Run: `./gradlew :core:lite:compileDebugJavaWithJavac`
Expected: BUILD SUCCESSFUL。

- [ ] **Step 3: 提交**

```bash
git add core/lite/src/main/java/com/baic/bridge/core/Bridge.java
git commit -m "feat(core): Bridge 门面暴露 register(module,cb)/register(module,ver,cb)/isReady"
```

---

### Task 9: 样例清单补 modules 字段（演示 + 真机验证 onConnected）

**Files:**
- Modify: `samples/navi-consumer/src/main/assets/bridge_nodes.json`
- Modify: `samples/account-provider/src/main/assets/bridge_nodes.json`
- Modify: `samples/media-provider/src/main/assets/bridge_nodes.json`

> navi-consumer 是消费方，其清单里列了它依赖的两个提供方节点，分别打上 modules。account/media 各自清单里对对方节点同样可打标（若有）。先看每个文件现有内容再按下例对齐 id。

- [ ] **Step 1: 给 navi-consumer 清单的提供方节点加 modules**

读取并把 `samples/navi-consumer/src/main/assets/bridge_nodes.json` 中提供 usercenter 的节点加 `"modules": ["usercenter"]`、提供 media 的节点加 `"modules": ["media"]`。例如：

```json
{
  "version": 1,
  "nodes": [
    { "id": "com.baic.usercenter", "action": "com.baic.usercenter.HOST",
      "component": "com.baic.usercenter/.HostService", "modules": ["usercenter"] },
    { "id": "com.baic.media", "action": "com.baic.bridge.NODE", "modules": ["media"] }
  ]
}
```

- [ ] **Step 2: 其余两个样例清单同样按节点 id 对应模块补 `modules`**

对 `samples/account-provider/.../bridge_nodes.json` 与 `samples/media-provider/.../bridge_nodes.json`：先读取现有内容，对其中出现的 `com.baic.usercenter` 节点补 `"modules": ["usercenter"]`、`com.baic.media` 节点补 `"modules": ["media"]`。无对应节点的文件可不改。

- [ ] **Step 3: 提交**

```bash
git add samples/*/src/main/assets/bridge_nodes.json
git commit -m "chore(samples): 节点清单补 modules 字段（演示 onConnected 归属）"
```

---

### Task 10: 全量编译 + 单测 + 真机验证

**Files:** 无（验证）

- [ ] **Step 1: 全量单测**

Run: `export JAVA_HOME=~/Library/Java/JavaVirtualMachines/liberica-17.0.18 && ./gradlew :core:lite:testDebugUnitTest`
Expected: PASS（Topics 3 + ModuleState 5）。

- [ ] **Step 2: 全量编译**

Run: `export JAVA_HOME=~/Library/Java/JavaVirtualMachines/liberica-17.0.18 && ./gradlew assembleDebug`
Expected: BUILD SUCCESSFUL，三个 sample apk 产出。

- [ ] **Step 3: 真机/多 App 验证（人工）**

按 spec §8 执行并在 logcat 过滤 `Bridge.Core`：
1. 装三个 sample，启动 navi-consumer → 见启动横幅（SDK版本/gitSha/构建/传输ABI/节点数）。
2. 后启动 account/media provider → 见「模块节点已连接」(onConnected) 紧接「模块就绪」(onReady)。
3. 杀掉再重启某 provider → 见「模块提供方已离线」(LOST) 后「模块提供方重启恢复」(onRebooted)；断开期间 `isReady` 返回 false。
4. provider 未起时主动 `request` → 见「request 调用时模块未就绪 … isReady=false」告警 + 收到 E_NO_PROVIDER。
5. 确认日志含「注册模块 X 契约门面版本=N」。

- [ ] **Step 4: 标记完成**

记录验证结果（通过/问题）。如全通过，本计划完成。

---

## 验证命令汇总

```bash
export JAVA_HOME=~/Library/Java/JavaVirtualMachines/liberica-17.0.18
./gradlew :core:lite:testDebugUnitTest   # 纯逻辑单测
./gradlew assembleDebug                   # 全量编译
adb logcat -s Bridge.Core Bridge.Conn     # 真机观察日志
```

## 备注

- `onConnected` 为 bind 边沿触发：若消费方在提供方已连接之后才 `register`，本次不补发 onConnected（但 onReady 会在 register 的补评估或下次握手时正确触发）。这符合「onConnected 仅作早期排查信号」的定位。
- 未声明 `modules` 的节点不影响 onReady/onRebooted/isReady（这些以握手 providedTopics 前缀判定），仅其 onConnected 不触发。
